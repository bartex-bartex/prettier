paragraphs - następujące linie tekstu oddzielone pustą linią (dopiero linia z whitecharacters = <br> EMPTYLINE?)

headers: h1 - h6; no need to close

?HTML? table

(zróbmy, że przed każdą linią trzeba dać >)
blockquotes, can have nested blockquotes, header, lists, code blocks, inline code, ?table? (but not every element can be used)

lists (can have nested link, image, italic, bold, inline code):
	unordered: * + -
	ordered:   1. 

code blocks (or indent without surrounding also possible)
```py
# code here
```

pozioma linia(conajmniej trzy, odzielone spacją, albo tabem):
---
***

link: [Kliknij](https://gars.pl)

zdjęcie: <img width='100' src="abc.jpg">

italic: *hejka lubie*    => <em>
bold:   **hejka lubie**  => <strong>
inline code: Use the <code>printf()</code> function.

\ escapes:
\   backslash
`   backtick
*   asterisk
_   underscore
{}  curly braces
[]  square brackets
()  parentheses
#   hash mark
+   plus sign
-   minus sign (hyphen)
.   dot
!   exclamation mark